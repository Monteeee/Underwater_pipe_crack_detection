import numpy as np


class BetterProposal:
    @staticmethod
    def better_sliding_window(image_shape, window_size):
        """this function serves demo by providing better sliding window.
        It should return a group of windows, i.e. a group of 4x2 ndarray showing pixel coordinate
        of window corners"""
        windows = list()

        return windows

    @staticmethod
    def better_proposal(pos_score_group):
        """this function serves demo by providing better proposals.
        It should receive a group of position-score pair, representing raw proposals for detection.
        Then this function will find better proposals by summarizing raw proposals"""

        better_prop = list()
        return better_prop

